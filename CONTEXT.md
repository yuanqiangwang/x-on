# x-on — 领域词汇表 (CONTEXT)

> 本文件是**词汇表**，只容纳 x-on 的领域术语与边界，不含实现细节。
> 实现决策记在 `docs/adr/`；范围与进度不写在这里。

## 应用 (App)

在 Windows 上可被启动的一个独立程序。启动器索引的最小对象。

## 索引 (Index)

从扫描根爬取、加上用户手动指定的便携应用，共同形成的 App 集合，构建后常驻内存，供搜索过滤。

## 扫描根 (Scan Root)

被爬取的一个开始菜单目录实例。共两个，递归爬取、不限深度：

- 用户级：`%APPDATA%\Microsoft\Windows\Start Menu\Programs`
- 公共级：`%ProgramData%\Microsoft\Windows\Start Menu\Programs`

## 快捷方式 (Shortcut / .lnk)

扫描根内的 `.lnk` 文件，是索引的**一种**原始来源（另一种是便携应用）。一个 App 通常对应一个或多个快捷方式。

## 便携应用 (Portable App)

无需安装、可在任意文件夹直接运行的程序。通过**手动指定其 exe** 进入索引，是索引的另一种原始来源（与扫描根内的 `.lnk` 相对，不属扫描根）。可附一个可选的显示名覆盖，供搜索匹配。

## AppInfo

索引中的一个条目，代表一个可启动的 App。携带显示名与启动路径（`launch_path` 为 `.lnk` 或 `exe`）。具体字段待收敛。

## 启动 (Launch)

通过某个 AppInfo 激活其目标 App。**快捷方式**（`.lnk`）：启动快捷方式本身，保留其自带的参数与工作目录；**便携应用**（`.exe`）：启动该 exe，并把工作目录设为 exe 所在文件夹（便携应用常依赖相对路径）。

## 唤出 (Activate)

由全局快捷键触发的那一次状态切换：把启动器从隐藏变为可见，居中到光标所在屏，获得焦点，清空并聚焦搜索框。

## 匹配 (Match)

查询串对索引的过滤与排序规则：大小写不敏感；前缀命中优先于子串命中；中文字符按字符直接匹配。

## 去重 (Dedupe)

多个开始菜单快捷方式解析到同一目标程序时，仅保留一个条目。**便携应用为独立条目**，不与同名程序合并。

## 唤醒 (Wake)

程序在后台常驻、仅在唤出时显示与交互的运行模型；对应窗口为不透明、无边框、置顶、不占任务栏。

## 托盘 (System Tray)

程序常驻时暴露在任务栏托盘的入口，用于"退出"与"重扫索引"，是常驻启动器的标准出口。

## 配置 (Config)

可配置的全局快捷键等设置，v1 暂以配置文件形式存放、启动时读取。

## 单实例 (Single Instance)

程序同一时刻只允许运行一个进程，以避免全局快捷键被重复注册。

## 范围 (Scope) — v1 边界

v1 做**开始菜单 App 启动器 + 手动指定的便携应用**：索引 `.lnk` 快捷方式，并索引用户手动指定的 exe（便携应用）。仅启动应用。文件搜索、网页搜索、计算器、任意命令等**均不在 v1 范围**，列为二期独立主题。
