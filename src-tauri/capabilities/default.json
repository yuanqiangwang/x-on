{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Main window capability: allow the webview to call our backend commands.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-hide"]
}
